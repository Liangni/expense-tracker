<div class="row justify-content-center">
  <div class="col-8">
    <div class="col-6">
      {{!-- category filter --}}
      <div class="dropdown">
        <button class="btn btn-info dropdown-toggle btn-block" type="button" id="dropdownMenuButton"
          data-toggle="dropdown" aria-expanded="false">
          類別
        </button>
        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
          <a class="dropdown-item {{#ifCond '' categoryId }}active{{/ifCond}}" href="?categoryId=">全部</a>
          {{#each categories }}
          <a class="dropdown-item {{#ifCond this._id ../categoryId }}active{{/ifCond}}" href="?categoryId={{this._id}}">{{this.name}}</a>
          {{/each}}
        </div>
      </div>
      {{!-- total amount --}}
      <div class="mt-5">
        <h5 class="my-0">總金額: </h5>
        <p class="display-4 text-right text-info">{{ totalAmount }}</p>
      </div>
    </div>
    <div class="col-12">
      {{!-- records --}}
      <table class="table table-striped">
        <tbody>
          {{#each records}}
          <tr>
            <th>
              <div class="row align-items-center">
                <div class="col-2">
                  <span style="font-size: 3em"><i class='{{this.categoryId.iconClass}}'></i></span>
                </div>
                <div class="col-6">
                  <h5>{{ this.name }}</h5>
                  <small>{{ this.date }}</small>
                </div>
                <div class="col-4">
                  <h5>{{ this.amount }}</h5>
                </div>
              </div>
            </th>
            <td class="align-middle">
              <a class="btn btn-link" href="/records/{{ this._id }}/edit">修改</a>
              <form action="/records/{{ this._id }}/?_method=DELETE" method="POST" style="display: inline;">
                <button class="btn btn-link" type="submit">刪除</button>
              </form>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
    {{!-- create button --}}
    <a class="btn btn-warning col-4 offset-1 mt-2 " href="/records/new">新增支出</a>
  </div>
</div>